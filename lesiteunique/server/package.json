{
  "scripts": {
    "start": "node server.js",
    "order66": "sudo docker compose stop; sudo docker compose rm --force ",
    "vador": "sudo docker compose build && sudo docker compose up"

  },
  "dependencies": {
    "express": "^4.18.2",
    "mysql": "^2.18.1",
    "mysql2": "^3.6.0"
  }
}
